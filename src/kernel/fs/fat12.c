#include <rhino/fs/fat12.h>
#include <rhino/arch/x86/timer.h>

/*static uint32_t calculate_serial(){
    time_t time = now();
    return ((((time.second << 8) | (100 / 2)) + ((time.month << 8) | (time.day))) + (((time.hour << 8) | (time.minute)) + time.year));
}*/

